package ru.job4j.ru.job4j;

public class Variable {
    public static void main(String[] args) {
        int age = 28;
        System.out.println(age);
        float height = 1.76f;
        System.out.println(height);
        long brick = 500000000;
        System.out.println(brick);
        double bacteria = 156.386421;
        System.out.println(bacteria);
        long space = 1000;
        System.out.println(space);
        boolean work = true;
        System.out.println(work);
        byte file = 16;
        System.out.println(file);
    }
}






//- возраст (подразумевается возраст человека);

//        - пол (обозначать будем символами - мужской сокращаем до М, женский до Ж);

  //      - рост (измерять будем в метрах);

    //    - количество кирпичей для постройки дома (небольшой одноэтажный дом площадью 100м2);

      //  - количество бактерий (измерять будем в КОЕ / г - такую величину можно встретить если почитать состав обычного йогурта, часто там можно встретить число 1 * 107 КОЕ / г);

        //- расстояние до космических объектов (измерять будем в км);

       // - разрешение на работу (рассматриваем всего 2 значения - или есть разрешение или его нет);

        //- размер файла (измерять будем в байтах).